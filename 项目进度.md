# 膳管家 APP (ShanGuanJia / Harmony_Foods) - 项目进度交接文档

> **致接手的 AI 助手 / 开发者：**
> 这份文档详细记录了本离线健康管理应用（HarmonyOS / ArkTS）截至目前的开发状态、代码结构和核心规范。当你接手本项目时，请先阅读此文档以快速恢复上下文，避免重复劳动或打破已有的架构设计。

## 1. 项目概况与核心原则
* **定位**：一款聚焦痛风、高血压等特定人群的离线优先（Offline-First）饮食营养管理工具。
* **应用名称**：膳管家（App Icon 为 216x216 绿白健康沙拉碗徽标）。
* **技术栈**：HarmonyOS / ArkTS 声明式 UI。
* **设计原则**：
  * **极简现代风**：主色调使用健康绿 (`#2E8B57`)，卡片式布局，大量采用系统级圆角和软阴影。
  * **零依赖原生**：为保证在各种环境下编译不报错，目前所有的高级 UI（如图表、复杂列表）**均使用 ArkTS 原生组件手搓，没有引入外部 ohpm 包**。
  * **严格类型安全**：已开启 ArkTS 严格模式编译，**全局禁止使用 `any` 和 `unknown`**（解决过 `arkts-no-any-unknown` 编译报错）。
  * **全量 PNG 图标**：全局图标采用预渲染高分辨率 PNG 格式，杜绝鸿蒙端 SVG 解析与兼容性问题。

## 2. 目录架构
核心代码集中在 `entry/src/main/ets/` 下：
* `common/` - 公共资源
  * `constants/AppTheme.ts` (统一定义了 PRIMARY_COLOR 等主题常数)
  * `components/DateNavigator.ets` (提取出的通用顶部日期切换器组件，带有左右箭头交互)
* `view/` - 五大核心 Tab 页面（**目前所有页面的 UI 骨架已 100% 完成**）
  * `home/HomeView.ets` (首页)
  * `food/FoodLibraryView.ets` (食物库)
  * `record/RecordView.ets` (记录)
  * `analysis/AnalysisView.ets` (分析)
  * `profile/ProfileView.ets` (我的)
* `entry/src/main/resources/base/media/` - 存放了全套规范的 96x96 / 216x216 PNG 格式图标与 App Icon。
* `entry/src/ohosTest/ets/test/FoodLibrary.test.ets` - 食物库逻辑验证单元测试。

## 3. 各模块已完成进度详解

### 3.1 首页 (HomeView)
* **状态**：UI + 真实 RDB 数据库联动 + 动态指标与个性化建议 100% 完成。
* **实现逻辑**：
  * **底层真实数据驱动**：接入 `FoodDBHelper` 本地数据库与 `NutritionCalculator` 计算引擎，自动统计并渲染今日实际摄入热量、三大宏量营养素克重与三大慢病风险指标。
  * **健康评分与环形进度条**：根据摄入的宏量供能比与慢病风险指标超标情况，动态计算健康综合评分（绿/橙/红自适应高亮），空数据态展示优雅占位。
  * **慢病指标三色预警**：嘌呤（痛风）、钠（高血压）、胆固醇（高血脂）根据医学阈值实时评估达标、偏高与超标状态。
  * **智能动态健康建议**：针对今日实际记录，优先定位超标指标并智能输出临床膳食建议；未记录时引导快捷录入。
  * **Tab 响应与交互闭环**：通过 `@Prop @Watch('onActiveTabChange')` 监听 Tab 切换，在「记录」页增删饮食后切回首页瞬间无感刷新；支持点击能量卡片与引导条直达记录，点击日期支持系统 `DatePickerDialog` 切换查看历史概览。

### 3.2 食物库 (FoodLibraryView)
* **状态**：UI + 前端逻辑闭环 完成。
* **实现逻辑**：
  * 去除了占空间且离线极难维护的食物实体图片，采用**纯排版极简卡片**，仅显示名称、热量、自定义动态红绿灯标签。
  * **分类切换与搜索**：已实现双向绑定逻辑，点击分类（如蔬菜、肉禽）或使用顶部 `Search` 搜索时，列表会实时计算 `getFilteredFoods()` 并精准过滤。
  * **自定义添加**：通过鸿蒙原生 `@CustomDialog` 实现了弹窗表单，提交后直接 `unshift` 入页面数据源并立即渲染。

### 3.3 饮食记录 (RecordView)
* **状态**：UI + RDB 本地数据库持久化 + 交互逻辑 100% 完成。
* **实现逻辑**：
  * **本地数据持久化**：底层采用鸿蒙关系型数据库 `@ohos.data.relationalStore`（`diet_record` 与 `favorite_food` 表），彻底废除写死数据，支持按日期隔离存储与全量增删查。
  * **动态日期与热量汇总**：支持点击 `<` / `>` 前后天切换及点击日期打开系统 `DatePickerDialog`，实时汇总当前日期的摄入总热量 (`总摄入 X / 2100 千卡`)。
  * **四大快捷入口闭环**：
    * 🔍 **搜索添加**：支持关键字检索内置与自定义食物库、切换餐段、自定义或预设克重（50g/100g/200g等），实时折算热量入库；
    * 🕒 **最近吃过**：自动去重读取历史吃过的食物，一键快速复用添加；
    * ⭐ **我的收藏**：从收藏夹中一键点选添加；
    * ⚡ **快捷录入**：无须食物库，支持自定义输入食物名称、份量与估算热量快速建档；
  * **餐段卡片与明细交互**：四大餐段（早/午/晚/加餐）右上角 `+` 按钮与空状态文本均可一键呼出对应餐段的添加弹窗；每条记录均提供确认删除功能，删除后自动刷新并扣减热量。

### 3.4 数据分析 (AnalysisView)
* **状态**：UI + 多维度时间联动 + 三大风险预警 + 柱状交互 + 周期评估卡片 100% 完成。
* **实现逻辑**：
  * **多维度时间联动**：原生实现了 "日/周/月" Segment 切换器，切换时三大图表、评分均分徽章与底部健康指导即时联动同步更新。
  * **营养素堆叠柱状图**：原生手搓碳水（橙）、蛋白（蓝）、脂肪（红）多色堆叠柱状图，支持点击任一柱条高亮并展开展示具体的摄入克重与热量供能比。
  * **三大慢性病风险指标超标红绿预警**：支持在“嘌呤 (痛风)”“钠 (高血压)”“胆固醇 (高血脂)”三者间自由切换，根据医学建议上限（如 300mg、2000mg）动态渲染超标告警红条与安全达标绿条，点击柱条弹出风险预警与调理提示。
  * **历史评分与动态均分**：柱顶清晰显示单项分值，右上角徽章随周期动态计算平均分（周均分 86、月均分 85、日均分 87）。
  * **周期健康评估与饮食指导卡片**：聚合周期日均热量、三大宏量营养素黄金供能比、超标预警次数统计，并智能生成科学调理方案。

### 3.5 我的 (ProfileView) 与全套独立设置/账号页面体系
* **状态**：UI + 全量独立 Page 化 + RDB 本地持久化 + 账号自增与认证体系 100% 完成。
* **实现逻辑**：
  * **完全独立 Page 架构（摒弃弹窗）**：将「我的」模块下所有核心功能全量重构为原生独立页面（共 10 大子页面）：
    1. `pages/EditProfilePage.ets`：个人资料修改（专属形象选择矩阵、昵称带统计输入、只读账号信息面板）
    2. `pages/BodyDataPage.ets`：基础身体数据（身高体重输入、BMI 实时动态标尺计算与体态分级、标准体重区间建议）
    3. `pages/HealthGoalPage.ets`：健康目标设定（减重/增肌/控尿酸/稳血压，每日目标热量调整与宏量营养黄金供能比）
    4. `pages/DiseaseTagsPage.ets`：特殊疾病标签（高尿酸/痛风、高血压、高血脂、脂肪肝、糖尿病等标签多选与风险阈值医学解读）
    5. `pages/ScoreWeightPage.ets`：评分维度权重自定义（均衡健康、风险指标优先、宏量营养优先三大模式对比与权重分布图）
    6. `pages/ExportDataPage.ets`：本地数据导出（数据概况总览、JSON 实时流预览、一键复制到剪贴板并更新备份时间戳）
    7. `pages/DataManagePage.ets`：恢复与重置数据（预置数据恢复确认、清空历史饮食记录确认、彻底重置出厂状态）
    8. `pages/AboutPage.ets`：关于页面（应用名称与版本 v1.0.0、离线优先理念、核心特性、隐私协议独立跳转入口）
    9. `pages/PrivacyPolicyPage.ets`：隐私政策与协议独立页（Offline-First 零云端上传原则、本地 SQLite 存储机制、数据导出与彻底注销擦除权利、医疗免责声明）
    10. `pages/LoginPage.ets`：极简纯粹登录页面（仅允许输入名字与密码、无默认账号密码预填、去除快速注册与一键体验冗余选项，根据名字自动识别或分配自增 ID）
  * **新增「系统与账号」卡片组**：
    * **关于膳管家**：点击直达 `AboutPage`；
    * **退出登录**：弹窗确认，保留用户历史数据，跳转至 `LoginPage`；
    * **注销用户**：强警示确认，彻底清空用户本地保存在 SQLite 的所有记录与收藏，重置系统并跳转至 `LoginPage`。
  * **自增用户 ID（从 10000 起）**：
    * 数据库维护 `user_account` 表（`userId INTEGER PRIMARY KEY AUTOINCREMENT`）；
    * 初始默认用户分配 ID `10000`，新注册用户自动从 `10001`、`10002` 依次递增；
    * 个人资料、编辑页与数据库严格联动展示真实自增 ID。
  * **严格响应需求**：已彻底移除原先的生物识别解锁组件。
  * **生命周期无感刷新**：利用 `Index.ets` 的 `onPageShow` 监听返回事件，任何子页面修改保存后回退至主页瞬间无缝刷新。

## 4. 下一步开发计划 (Next Steps)
当你（另一台电脑或AI）接手时，可以按照以下顺序推进实质性业务功能的开发：

1. **数据库层建设 (Data Persistence)**：
   * 引入鸿蒙原生的 **RDB 关系型数据库** (`@ohos.data.relationalStore`)。
   * 建立 `Food` 表、`DietRecord` 表、`UserProfile` 表，实现底层 SQLite 增删改查逻辑。
2. **状态管理升级 (State Management)**：
   * 将当前页面内写死的 `@State mockFoods` 替换为应用级状态管理（`AppStorage` 或 `PersistentStorage`），连接上层的 UI 和底层的 RDB。
3. **功能打通 (Logic Integration)**：
   * 食物库中“点击自定义添加” -> 存入数据库 -> 在所有引用处实时刷新。
   * 记录页面中“点击 ＋ 号” -> 跳转选库 -> 选择克重 -> 回写今日数据 -> 动态刷新首页的环形图占比。
4. **扩展数据安全闭环**：
   * 真正使用系统 API 完成“本地 JSON/CSV 导出”逻辑以及本地文件的读写。

---
*Generated with Google Antigravity Agent, 2026-09*
